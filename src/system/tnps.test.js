import { assert } from 'chai'
import { scale } from '../..'
import { getIntervalsAtPosition } from './tnps'

describe('some test', () => {
  it('should do something', () => {})
})

// scale()
//   .tuning('EADBGA')
//   .system(tnps)
